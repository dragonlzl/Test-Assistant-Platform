# AI 用例编写风格与人工特征参考

## 适用范围

本指南用于约束 AI 生成测试用例时的文字风格，覆盖普通用例生成、XMind 用例生成、用例库 AI 补充生成、执行页 AI 补充生成等入口。

生成目标不是把用例写成标准化说明文档，而是在保证质量、字段完整、优先级合规、语义不重复的前提下，尽量贴近正式入库用例里测试人员的人工编写习惯。

## 人工用例更常覆盖的测试点

人工用例通常不只覆盖需求明面规则，还会补充历史缺陷、真实入口、玩家异常操作、旧逻辑兼容和状态闭环。AI 生成时应根据需求类型主动判断是否需要补充以下类型，不要机械地每类都生成。

### 历史兼容与旧入口

- 覆盖旧入口、旧 UI、旧购买链路、旧热区是否被正确屏蔽或跳转。
- 覆盖新旧逻辑复用时的差异，例如新商城复用老商城预览逻辑但隐藏独立购买按钮。
- 覆盖历史功能是否被误影响，例如活动外大厅还原、老商店入口替换、旧直购入口不可误触发。

### 真实入口矩阵

- 同一功能如果有多个入口，要按入口分别测落点、默认页签、弹窗、红点、是否消耗首次次数。
- 常见入口包括主界面按钮、NPC、资源栏、邮件、公告、活动入口、礼包跳转、资源不足跳转、商城 banner、背包按钮、任务栏、引导链路。
- 定向入口要校验优先级，不应被上次浏览页签、历史缓存或默认入口策略干扰。

### 跨模式与跨场景兼容

- 游戏内功能要结合具体模式名和高风险场景，而不是只写“特殊模式兼容”。
- 常见场景包括普通关、吊炸天、赛季、守护神殿、迷迭岛、邪王、虚空、试炼、PVP、联机、Boss 房、特殊地形、跨房间、跨层、结算界面。
- 技能、宠物、武器、活动因子、奖励结算等要关注是否带入不该生效的模式，或在目标模式中丢失效果。

### 玩家异常操作与稳定性

- 覆盖快速连点、重复点击同一按钮、重复进入退出、连续领取、连续购买、滑动中切页、惯性滚动中切页、多指同时操作。
- 覆盖取消、关闭、返回、切后台、锁屏、断网、重连、重启、重登后的状态恢复。
- 对弹窗、购买、兑换、三选一、出战替换、筛选切换、实机演示等容易状态错乱的功能，应补稳定性用例。

### 存档与状态持久化

- 覆盖云存档上传/下载、本地缓存清理、重启游戏、重新进入页面、跨局、跨房间、跨日、跨活动周期后的状态一致性。
- 重点检查一次性发放、首次弹窗、每日次数、兑换状态、按钮位置、出战配置、成长数据、临时增益是否正确保存或清除。
- 需要区分“局内临时状态”和“局外永久状态”，例如局内增益对局结束清空，已捕获或已购买内容局外保留。

### 资源经济与奖励闭环

- 覆盖领取、购买、兑换、分解、返还、扣费、到账、次数减少、余额刷新、货币上限、库存为空、全部拥有、已兑换、货币不足。
- 覆盖取消确认不生效、失败不扣费、成功到账、重复操作不重复发放、临界余额不溢出。
- 覆盖奖励和货币的展示位置，例如结算界面展示、保险柜不展示、本地存档不展示、背包刷新、红点消失。

### 配置异常与兜底

- 覆盖空配置、缺字段、非法编号、异常品质、候选池不足、概率为 0、概率为 100、全档位映射、相邻档位切换。
- 对配置驱动功能，应校验配置表字段、资源映射、版本号、开启/关闭时间、权重、池子来源、候选去重、随机分布。
- 对脏数据或异常配置，要写不崩溃、不空白、不乱码、不取未定义值、按兜底策略处理。

### 多人同步与归属

- 联机或多人场景要校验主机/从机、自己/队友、第三人称观察、同步显示、归属隔离。
- 常见风险包括队友子弹不应被吸附、自己的召唤物只受自己控制、多人三选一只弹自己、双方放置物互不相连、候选结果是否一致或不一致。
- 预期要写清“谁能看到、谁能操作、谁的状态变化、是否同步给其他玩家”。

### 视觉听觉文本与设备适配

- 覆盖图标、模型、动画、特效、音效、视频、实机演示、红点、按钮置灰、进度条、倒计时、空态、文案变量。
- 覆盖多语言、字体切换、特殊分辨率、长文本超框、音画同步、前后台恢复播放、资源缺失兜底。
- 对可预览内容，要测多次切换、关闭返回层级、资源释放和重载。

### 产品细节与已知高风险点

- 人工用例常包含项目知识，例如具体 NPC、关卡名、Boss 名、特殊地形、角色皮肤、雕像、因子、彩蛋、渠道差异。
- 如果需求或已有用例中出现这些业务名词，生成时应保留并围绕它们补检查点。
- 不要把高价值项目细节泛化成“特殊场景”，应写成可执行的具体场景。

## 按需求类型选择补充方向

- 技能/战斗类：重点补模式兼容、联机同步、死亡复活、跨房间、特殊地形、天赋/因子/雕像联动、数值边界、视觉音效残留。
- 商城/付费类：重点补入口矩阵、AB/渠道差异、资源不足、二次确认、支付取消/失败、老入口屏蔽、购买后刷新、已拥有/全部拥有、云存档恢复。
- 活动/奖励类：重点补开启关闭时间、未达门槛、每日次数、跨日重置、结算模式、兑换上限、奖励到账、活动结束后继续游戏、存档恢复。
- 背包/养成类：重点补容量/槽位上限、排序刷新、替换取消、重复操作、出战/休息状态、删除或分解后的引用清理。
- UI/引导类：重点补首次/再次进入、红点消失、引导占用、切页恢复策略、滑动中切换、多语言超框、特殊分辨率。
- 随机/配置类：重点补池子来源、权重、概率 0/100、候选不足、全档位、异常配置兜底、统计分布。

## 生成策略

- 先覆盖需求明示的 P0 主链路，再补人工高频漏点，不要让特殊场景挤掉核心规则。
- 对每个模块优先补 1-3 条最有风险的人工特征用例，避免把所有通用风险机械套到每个模块。
- 如果已有用例覆盖了主流程，补充生成时优先生成入口矩阵、异常操作、状态恢复、配置边界、跨模式兼容。
- 对需求中没有依据的场景，不要编造具体数值、具体渠道或具体资源名；可以写“按配置”“目标渠道”“测试数据”。
- 对需求或已有用例里出现的具体业务名词，要尽量保留，形成可执行检查点。
- 预期结果要包含关键状态变化：到账/扣除/刷新/保存/清除/不重复/不残留/不串页/不误触/不继承。

## 总体风格

用例整体应保持短、业务化、检查点化、不过度模板化。

- 模块名像业务分类，不像长标题。
- 用例标题像检查点，不像完整验证句。
- 前置条件写测试状态，不写背景解释。
- 操作步骤多用一两句直接动作，不展开成长流程说明。
- 预期结果写可观察结果，不写理论解释。
- 保留项目内常用业务词、简称和测试人员口吻。

## 字段写法

### module

- 使用短业务分类，例如：登录、支付、技能效果、奖励兑换、任务系统、装备养成。
- 可以保留项目组自己的编号和层级习惯，例如：1、技能、7、专精与专精之间的搭配。
- 不要把完整场景、步骤或预期塞进模块名。

### title

- 标题优先控制在 4-12 个字左右。
- 写成功能点或检查点，例如：入口、奖励领取、装备分解、支付方式、活动ui、任务面板。
- 不要批量写成“验证 XXX 功能是否正常”。
- 允许同名标题重复，通过前置条件、步骤、预期区分场景。
- 标题末尾一般不加句号。

### priority

- 只能填写 P0、P1、P2。
- P0 是最高优先级。
- 默认多数用例使用 P1。
- 核心链路、登录、支付、严重阻断、版本主功能、关键奖励发放使用 P0。
- 边界、低频、兼容、展示细节、非主流程分支可使用 P2。
- 不允许输出高、中、低、p1、p2、装备名、效果名或其他非标准值。

### preconditions

- 写当前测试状态，保持短句。
- 常见写法：活动已开启、已进入对局、解锁角色、打开战令、玩家已拥有皮肤。
- 有多条时可用中文序号分点。
- 不写大段业务背景，不解释为什么测试这个点。

### steps

- 优先写单行动作，必要时再分点。
- 常用动作：进入、点击、查看、观察、检查、选择、装备、领取、购买、重复、重登。
- 允许测试人员口吻，例如：查看是否开启活动、观察内容展示、点击兑换后查看弹窗表现、装备后平A稻草人观察。
- 不要把每条都扩写成很长的教学步骤。

### expected

- 写直接可观察结果。
- 常用表达：正常、正确、显示、展示、提示、可、不可、不会、到账、扣除、刷新、一致。
- 示例风格：正常显示、展示正确、商品正常到账、不会展示货币及数量、与配置一致。
- 预期末尾一般不加句号。

### remark

- 默认留空。
- 只有确实需要补充说明时才填写。

## 人工感要求

- 可以使用项目业务黑话和简称，例如：平A、红武、词条、专精、保底、回流、pvp、ui、重铸、羁绊。
- 可以保留轻微人工差异，例如 ui/UI、短句、重复标题、偶尔编号。
- 不要为了统一而把所有用例改成同一种句式。
- 不要生成过度工整的模板化句子。
- 不要为了显得完整而增加无价值步骤。

## 质量底线

风格模仿不能牺牲用例质量。

- 字段必须完整。
- priority 必须严格为 P0/P1/P2。
- 用例之间不能明显重复。
- 步骤和预期必须能落地执行或观察。
- 涉及数值、配置、奖励、付费、登录、状态变化时，预期要写清楚关键校验点。
- 不确定是否该覆盖时，优先保留有测试价值的用例。

## 生成前自检清单

生成前按需求类型快速自检：

- 是否覆盖核心成功链路和失败链路。
- 是否存在多个入口或定向跳转。
- 是否涉及已拥有、未拥有、全部拥有、资源不足、次数耗尽。
- 是否涉及时间、每日次数、跨日、活动开启/关闭。
- 是否涉及云存档、本地缓存、重登、重启、跨局或跨房间。
- 是否涉及联机、PVP、赛季、神殿、迷迭岛、虚空、邪王等模式。
- 是否涉及配置池、概率、权重、档位、非法配置或兜底。
- 是否涉及快速连点、多指、滑动中切页、取消、关闭、返回。
- 是否涉及多语言、特殊分辨率、音效、演示、视频、资源加载。

## 生成时的简短指令

生成用例时模仿测试人员从 XMind 或表格里整理检查点的风格：模块短、标题短、前置短、步骤多为单行动作、预期是直接可观察结果；在覆盖需求明示规则后，按需求类型补入口矩阵、旧逻辑兼容、异常操作、存档状态、资源闭环、配置兜底、联机归属、模式兼容等人工高频漏点；保留项目业务词和轻微人工写法差异，但优先级严格只用 P0/P1/P2。
