# HTML 页面拆分现状

## 当前页面

1. `ai-workflow.html`：仅承载 XMind 用例生成和其通用入库抽屉。
2. `ai-tools.html`：功能指派和模型管理。
3. `case-exec.html`：用例执行和执行总览。
4. `case-library.html`：用例库和用例归档。
5. `admin.html`：项目、人员和操作记录管理。
6. `settings.html`：保留配置。

`index.html` 是轻量跳转壳。旧 `auto`、`clean` 和 `xmind-casegen` 参数会归一为 `casesgen`，历史书签和已保存页签不会进入空白页面。

## 初始化边界

- 页面继续按 vendor、config、core、base、services、handlers、modules、bootstrap 的依赖顺序加载。
- `retainedGenerationRuntime` 统一装配 XMind、用例库内生成、执行页内生成和易漏用例提醒所需任务管理器。
- `casesGenCore` 暂作为 XMind 的内部兼容依赖，保留设置、快照、模块提交和通用入库能力，不再拥有菜单或独立操作页。
- 页面模块必须允许缺少非本页 DOM，不能恢复一键执行、功能流程或普通模块生成入口。

## 回归清单

- 默认入口与历史地址均打开 XMind 用例生成。
- 所有页面不显示一键执行和功能流程菜单。
- XMind 多工作区、导入、生成、中断/刷新恢复、去重、覆盖、导出、新建入库和追加入库可用。
- 用例库和执行页内生成与追加可用。
- 模型管理和四类保留能力指派可用，废弃配置不会重新写回。
- 登录、登出和历史页签恢复不会出现空白页面。

## 已下线范围

- 一键串联、评审、清洗、完整性对比、测试模块拆分和覆盖对比页面。
- 旧版普通模块生成、批量生成、调试、通知、页面说明和功能引导。
- 上述能力的菜单、页面结构、专用脚本和专用测试。
