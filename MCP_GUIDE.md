# Codex MCP 接入

平台在现有服务上提供 `/mcp`，使用 Streamable HTTP 的无状态 JSON 响应模式。支持协议版本 2025-06-18 和 2025-03-26，客户端可在初始化时协商版本；不提供旧 HTTP+SSE、通知流、模型代调用或自动化测试执行器。

Codex App / CLI 使用成员自己的模型账号。MCP 只提供平台业务工具，不需要平台接收成员的模型 API Key，也不绑定 GPT 的具体型号。

## 个人接入

1. 登录平台，进入“设置 → AI 与知识库 → Codex MCP 接入”。
2. 填写设备名称和有效天数，按需勾选“仅允许读取”，生成凭据。完整凭据只显示一次，服务器只保存哈希。
3. 在本机 Codex 添加远程 MCP 服务，地址为 `https://你的平台域名/mcp`。使用自己的凭据，不使用共享管理员凭据。

### CLI（环境变量方式）

先通过本机安全的凭据管理方式设置 `TAP_MCP_TOKEN` 环境变量，不要把真实值写进对话、仓库或提交记录。

```sh
codex mcp add test-assistant --url https://你的平台域名/mcp --bearer-token-env-var TAP_MCP_TOKEN
codex mcp list
```

在 Codex CLI 中运行 `/mcp` 检查连接。环境变量需要存在于启动 Codex 的进程环境中。

### Codex App

在 MCP 设置中添加 Streamable HTTP 服务，使用相同地址。App 与 CLI 在同一个本地 Codex host 下共享 MCP 配置。若 App 是从桌面启动，它未必继承终端的环境变量：可以从已设置变量的终端启动 App，或在**仅本机的个人 Codex 配置**中设置认证 Header。

```toml
[mcp_servers.test-assistant]
url = "https://你的平台域名/mcp"
bearer_token_env_var = "TAP_MCP_TOKEN"
```

如果改用本机配置中的静态 Header，应删除 `bearer_token_env_var`，使用：

```toml
[mcp_servers.test-assistant]
url = "https://你的平台域名/mcp"
http_headers = { Authorization = "Bearer <你的个人 MCP 凭据>" }
```

只选一种凭据来源。静态 Header 包含秘密，请保护本机配置文件；不要放进项目共享配置。修改配置后在 App 中重启 MCP 连接。第一版使用个人 Bearer Token，**不使用 `codex mcp login`**（该命令用于 OAuth 服务）。

官方说明：https://developers.openai.com/codex/mcp

## 功能与参数

所有工具都有严格的参数 schema，拒绝未知字段。业务工具要求 `project_id`，服务端仍会根据凭据验证项目及目标对象归属，不能通过填写另一个用户 ID 改变身份。

| 分组 | 工具 |
| --- | --- |
| 身份、项目、版本 | get_current_user、list_projects、list_versions、create_version |
| 用例 | list_case_files、get_case_items、search_cases、create_case_file、append_case_items、update_case_item |
| 执行 | create_execution_set、list_execution_sets、get_execution_cases、record_execution_result、get_execution_overview |
| 复用子项 | get_execution_reuse_context、add_execution_reuse_presets、update_execution_reuse_presets、quick_execute_reuse |
| 归档、历史 | list_archives、get_archive、get_case_history |
| 易漏用例 | list_missing_cases |
| 知识库 | list_knowledge_bases、list_knowledge_documents、search_knowledge、get_knowledge_document |
| 操作记录（仅管理员） | list_operation_logs、get_operation_log_detail、get_operation_log_summary |

同时提供 `tap://standards/case-writing` 和 `tap://standards/fields` 规范资源。

MCP 初始化时会自动下发与 XMind 共用的完整人工编写风格指南；新建、追加和修改用例工具的说明及字段描述也包含风格要求，无需每次在对话里重复强调。生成用例时模块用短业务分类，标题优先 4–12 个字，前置只写当前状态，步骤优先单行动作，预期写可观察结果，备注默认留空；涉及数值、扣费、到账和状态变化时仍须保留关键校验点。补充前先读取已有用例，按需求补漏并做语义去重。

新增前还必须做单条复杂度自检：多个独立目标、互斥前置或成功/失败/取消等分支集中到一条，导致描述过长复杂、难以阅读时，先精简，再按目标拆成多条可独立执行的用例。同一目标的必要连续操作和关键结果仍保留，不按固定字数机械切分。先拆分再对最终候选查重；取得相似对比令牌后再拆分或改写，须重新查重确认。这是 AI 生成上下文要求，服务端不按字数自动截断或拆写已提交、已存储的用例。

MCP 仍使用 `precondition` 和字符串类型的 `steps`（多步用换行）；风格指南中的 `preconditions` 不改变 MCP 字段协议。规范用于指导 AI 生成，不会自动截断、重写已存用例或强制限制标题为 12 个字。更新服务后需重新连接 MCP，让客户端刷新初始化上下文和工具描述；无需清理网页缓存。

- 列表通常用 `limit`（默认 50、最多 100）和 `offset` 分页；`next_offset=null` 表示结束。
- 操作记录例外：`list_operation_logs` 默认最近 7 天、20 条摘要，最多 100 条，使用 `next_cursor` 翻页。支持日期、人员、操作类型、结果和对象筛选；单次最多 366 天。保留原筛选条件传入 cursor，更改筛选时清空 cursor；不要自动遍历所有页。
- `get_operation_log_detail` 按日志 ID 和字符 offset 读取详情，默认 6000、最多 12000 字符，只有摘要不足时调用。`next_offset=null` 表示读完，返回的是 JSON 文本片段。
- `get_operation_log_summary` 传入 `user_ids`（1–100 人）和 view（activity/contribution/execContribution）获取计数汇总，默认最近 7 天；统计问题优先使用此工具，无需拉取逐条日志。三种操作记录工具均实时校验管理员角色，普通成员返回没有权限。
- 查询版本不传 `version_id` 表示不限版本。创建执行集不传则沿用用例文件版本，显式 null 表示未分配版本。
- 所有写工具要求 `idempotency_key`（8–128 字符）。一次逻辑操作使用一个 UUID，网络重试使用相同键和完全相同的参数；同一用户复用不同内容的键返回 CONFLICT。
- 修改用例、追加用例和记录结果要求 `expected_updated_at`，使用刚读取的对象 `updated_at`。冲突后应重新读取并重新决定操作，不能盲目重试覆盖。
- 新建、追加每批最多 200 条。整批业务变更与幂等回执在同一数据库事务中提交，失败不部分写入。发现相似项先返回 `CASE_REVIEW_REQUIRED`，用户选择后再写入；确认继续新增时，同文件完全重复项仍跳过并返回计数。
- 用例库及执行页在“增删”左侧显示只读的“AI操作”列。MCP 实际新增条目记录“AI新增”，新增复用子项记录“AI新增子项”，实际修改内容记录“AI修改”，修改执行结果（包括复用子项）记录“AI执行”；无操作时为空，多种操作累积显示。API 返回 `ai_operations` 数组，值为 `created`、`child_added`、`modified`、`executed`，写入参数不接受该字段。
- 标识随用例持久保存，并同步到来源用例及关联执行记录；人工编辑、同步、归档恢复和平台内分享保留标识，不会改变其他执行记录的结果。无变化写入、追加跳过的重复项、失败或回滚不新增标识，幂等重试不重复累积。删除的用例不会把标识传给新建条目；历史执行副本仍保留自身标识。
- 创建执行集遇到相同用户、文件和版本的现有活动集时直接返回，不进行隐式同步。
- 记录结果只修改状态、实际结果、备注、缺陷链接；不会触发用例内容回写或自动入库。测试人员/外部执行器提供真实结果后才应记录，平台不会实际运行被测程序。
- 复用用例记录结果时，必须指定刚读取的 `reuse_detail_id`，可附 `reuse_note`；只更新该子项并按现有规则汇总主状态，清除该子项自动适用性标记，保留其他子项。预设子项和解锁方式也可通过下面的 MCP 工具维护。
- 执行列表默认个人可见，执行明细沿用项目内读取规则；写入仅所属用户或管理员。总览口径沿用网页，包含归档记录。
- 请求体上限 2 MiB；单次业务结果上限 250000 字符。结果过大时缩小查询或减小分页条数。
- MCP 工具错误使用 `isError=true`，包含 `PERMISSION_DENIED`、`CONFLICT`、`NOT_FOUND` 等结构化错误。无效或过期凭据在传输层返回 HTTP 401；业务权限不足不触发重新登录。
- 凭据只读模式会隐藏写工具，同时在执行入口强制拒绝写入。项目成员关系、管理员角色和账号状态每次调用重新读取；移出项目立即影响后续请求，包括幂等结果重放。
- 用户改密、管理员重置密码、账号停用都会撤销已有 MCP 凭据。普通网页登录/登出与 MCP 凭据分开，MCP 凭据可在设置中单独撤销。
- 操作日志记录实际用户、凭据 ID/名称、客户端 User-Agent（仅作审计标签）、工具、目标 ID、结果及是否重放；不记录完整凭据或文档正文。

## 新增用例前的相似确认

`create_case_file` 自动检查当前项目的已有用例（包含其他文件和版本）；`append_case_items` 检查目标文件内用例。首次按原有参数调用，无相似项直接入库；命中时返回 `isError=true` 和 `error.code=CASE_REVIEW_REQUIRED`，**整批业务数据未写入，也不保存成功幂等回执**。

返回结果包含 `review_token`、检查范围、待确认的 `item_index`（从 0 起）、相似用例 ID/文件/版本、匹配原因、分数及 `differences`。每条最多返回分数最高的 3 个匹配，并标注 `match_count`、`matches_truncated`；字段摘要超过 240 字符时有 `truncated=true`。摘要不足时，先用 `get_case_items` 读取全文。

AI 客户端必须把对比展示给用户，并等待明确选择：

- `update`：修改选中的已有用例。只更新本次候选中显式提供的字段，未传优先级、前置、备注等保持原值；实际改变时累计“AI修改”。
- `add`：保留已有用例并继续新增，实际新增时累计“AI新增”。同文件五个内容字段完全重复仍按原规则跳过，不制造重复条目。
- `skip`：跳过本次候选，保留已有用例。

确认后再次调用原工具，保留原目标、`items` 和 `idempotency_key`，仅补充：

```json
{
  "similarity_review": {
    "review_token": "使用上次对比返回的64位令牌",
    "decisions": [
      {"item_index": 0, "action": "update", "case_item_id": 123},
      {"item_index": 2, "action": "add"},
      {"item_index": 3, "action": "skip"}
    ]
  }
}
```

每个待确认项必须有且只有一个选择。修改目标须为该条返回的匹配，不允许同批多条覆盖同一目标；未命中相似的候选正常新增。确认后新增、修改、跳过作为一个批次提交，返回 `review_summary`（已修改 ID、无变化 ID、跳过下标）。若新建请求全部选择修改/跳过，不创建空文件，返回 `created=false`；追加全部修改/跳过则返回 `appended=0`。

令牌绑定 MCP 凭据、原始请求（包含幂等键及显式字段）和检查范围的数据快照。候选、目标或已有数据变化后须重新对比；目标文件版本变化会先返回 `CONFLICT`，重新读取版本后再发起查重。成功后的网络重试保持**包含确认结果的完整参数**及同一幂等键。

相似检查使用本地规范化文本及字符片段相似度，要求标题结合步骤/预期，或步骤和预期同时高度相似；同标题、同模块本身不会判相似，不调用模型供应商。此规则不能保证识别所有语义重复，客户端仍需结合业务判断。对话询问由 AI 客户端负责，服务端校验令牌及选择，不能独立证明用户在外部对话中已经点击确认。

为限制开销，单次最多扫描 10000 条已有用例、400 万字符、10 万对候选；超过限制直接拒绝且不写入。每批最多 30 条需要确认的候选，对比正文最大 20 万字符；超过时缩小批次，不会静默省略待确认用例。这一流程只作用于 MCP 新增/追加，网页导入及直接编辑保持现有规则。

部署此流程无需新增数据库表。重启后端并重新连接 MCP 即可加载新增参数和确认指令，不需要清理网页缓存。

## 复用子项与快速执行

四个工具均需 `project_id`、`exec_set_id`，写工具另需唯一 `idempotency_key` 和刚读取的 `expected_revision`。仅活动且已开启复用的执行集可写，所属用户或管理员有权操作；项目内其他成员可读，只读凭据隐藏并拒绝写工具。

1. `get_execution_reuse_context` 返回现有 `presets`、可用 `profile.options`、`revision`、用例数和上限，不返回所有用例正文。`profile=null` 表示当前执行集不支持自动匹配方式，但仍可新增普通预设子项。
2. `add_execution_reuse_presets` 的 `items` 为 `[{ text, applicability?: { profile, value } }]`。每个预设都会向执行集全部用例追加一个独立子项，初始未执行；同名或批次重名报冲突，不覆盖已有内容。`quick_execute=true` 可在同一事务中对本次新子项应用规则。
3. `update_execution_reuse_presets` 的 `items` 为 `[{ preset_id, applicability: { profile, value } | null }]`，null 清除方式。默认只保存配置；`quick_execute=true` 同时应用到这些子项，并恢复它们旧的自动不适用状态。
4. `quick_execute_reuse` 按已有配置执行规则，`preset_ids` 可限定预设范围，省略则处理全部。与网页“快速执行”一致：不匹配的未执行子项设为不适用，条件变化后恢复原先自动设置的不适用；人工通过/失败/阻塞/不适用、备注及已移除子项均保留。不会把匹配项标为通过，也不会运行被测程序。

获取/解锁方式沿用网页现有规则，目前只对“元气骑士”项目识别角色皮肤（碎片、宝石、付费、小鱼干、赛季商店、老皮肤赠送、条件解锁）和武器进化皮肤（扭蛋、小鱼干商店、盲盒、邮箱）。以读取工具返回的 profile/value 为准，不推测其他项目的业务规则。

写接口最多处理 2000 条执行用例、100 个总预设；一次新增或修改最多 50 个预设，新增子项总数（用例数 × 新增预设数）最多 20000。整批原子提交，失败不部分写入；MCP 重试沿用同一键和参数。`revision` 包含执行集及所有执行用例版本，网页修改结果、新增/删除用例或调整配置后，旧版本会返回 CONFLICT，需重新读取确认后再操作。

返回值包含新 `revision`、`affected_preset_ids` 和 `summary`（新增预设/子项数、更新用例数、自动不适用数、恢复数、保留人工结果数）。具体子项 ID 用 `get_execution_cases` 分页读取后，可继续调用 `record_execution_result` 记录真实结果。复用配置不回写共享用例正文；MCP 新增子项记“AI新增子项”，实际规则变更记“AI执行”，来源标识沿用现有累积规则。仅新增子项不再产生“AI修改”；历史“AI修改”标识继续保留，不自动重分类。

示例指令：“使用平台 MCP，给我的执行集 108 新增‘骑士新皮肤’预设子项，解锁方式为小鱼干，随后快速执行，只返回处理数量。”AI 应先读取配置，使用返回的选项和 revision 构造写请求。升级后重启平台并重新加载 MCP 即可发现这四个工具，本次无需新增数据库字段。

## 项目知识库与旧配置迁移

管理员在同一个设置分类的“项目知识库”面板中选择项目，填写名称及来源地址并登记。普通成员只能选择自己项目下的已授权知识库。管理员按现有平台管理权限访问所有项目。

每个来源登记都属于一个项目；同一来源允许显式登记到多个项目，从而实现有意共享。每个来源目录必须只包含允许向这些项目共享的文档，不支持在同一来源目录内部再按文档设置不同权限。不同权限域应使用独立目录及清单。

历史个人 `knowledgeBaseBaseUrl` 只作为选择偏好保留，**不会自动取得访问权限，也不会自动登记成全员共享**。管理员需按真实归属登记，用户从授权列表重新选择。没有登记时网页和 MCP 都返回没有权限，不会回退到任意 URL 抓取。

网页的校验、目录、检索、正文读取，以及 MCP 知识工具共用项目鉴权。MCP 接受项目和知识库 ID，不接受源站 URL。文档 ID 必须属于该授权来源目录，服务端缓存返回前仍需通过鉴权。网页自动复用已下载知识上下文前也重新检查权限；系统不能撤回用户在授权期间已经下载或复制的数据。

**源站访问要求**：知识库源站应只允许平台服务器访问（如内网 ACL、防火墙或反向代理限制），或具备自己的同等身份权限控制。平台无法保护成员可以绕过平台直接访问的公开源站。返回的文档原始来源链接也必须遵循这一要求。

## 部署

AI 操作标识使用增量迁移 v30，为用例库条目和执行条目补充 `ai_operations` 列。旧记录默认空数组，不推测或回填历史来源；重启后端自动补列，刷新网页加载新版列表和样式即可。MCP 客户端重新连接后可读取新增字段说明。

沿用现有启动方式，新增表在平台启动时创建，不改写既有用例。先在测试库演练并备份正式数据库，然后更新部署并重启服务。

操作记录查询优化由迁移 v29 为旧日志表补齐时间、人员、行为和对象索引；首次启动时创建，大表可能延长本次启动时间。升级后刷新网页加载静态脚本，并重新加载 MCP 以发现三项操作记录工具。日志数据保留，单次查询限窗不删除历史；查更早记录时明确指定日期。

可对 AI 说：“使用我配置的平台 MCP 查询最近 7 天失败的操作记录，只返回前 20 条摘要，不自动翻页；需要时再读取指定记录详情。”统计则说：“汇总指定人员本周的用例贡献，只返回计数。”

新增表：mcp_tokens、mcp_write_receipts、knowledge_sources。现有依赖即可运行 MCP 服务，不为接入而升级 FastAPI/Starlette。SQLite 的写事务使用 BEGIN IMMEDIATE，适合当前团队规模；忙锁会返回可重试结果。

用已有 HTTPS 反向代理转发 `/mcp` 和 `/api/`，保留 Authorization、Accept、Content-Type、MCP-Protocol-Version，不缓存响应；知识检索可配置 180 秒读取超时。Codex 直连不需要浏览器 CORS。服务拒绝未授权的 Origin；如确需浏览器 MCP 客户端，通过 `MCP_ALLOWED_ORIGINS=https://明确允许的域名` 设置准确来源（逗号分隔，不支持通配符）。

静态发布限制为网页及资源白名单，禁止通过 HTTP 获取数据库、服务端源码、本地配置、凭据和仓库隐藏文件。请同时确保外部反向代理没有绕过应用直接把整个仓库作为静态目录公开。

模型列表代理限定为不带查询参数的标准 `/models` 地址，防止作为任意 GET 代理读取知识库；现有前端模型发现使用该标准地址，不影响正常发现流程。

网页新增凭据与知识库管理控件，部署后刷新页面以加载新版静态资源。旧客户端知识库未完成项目登记前会收到拒绝访问。

部署完成后使用两名属于不同项目的普通用户验证越权拒绝，并分别在 Codex App/CLI 验证工具发现、资料读取、用例入库、执行记录和撤销凭据。不要在正式数据上造测试用例。
