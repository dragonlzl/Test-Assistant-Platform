# 新功能开发&问题处理要求（必读）  

> **重要：所有新增/调整的功能完成后，必须在 `FEATURE_LOG.md` 记录功能描述、操作方式、效果与新增内容；漏记视为未完成。**  

> 本文为新增功能的统一要求，开始开发前请先阅读并遵循。所有说明均为强约束，需按步骤执行并同步结果。

> 开始实现前，先阅读并对比 `FEATURE_LOG.md` 与当前计划，明确已有进展与待办，保证连续性（多次迭代时务必先对齐上下文再动手）。

## 开发原则  
1. **优先复用**：新增功能需尽量复用现有逻辑、接口或组件；仅在无法复用时再新增实现，避免重复与冗余。  
2. **可复用设计**：如需编写新代码，应考虑通用性/组件化，便于后续同类需求直接复用或扩展。  
3. **最小增量**：评估影响范围，避免牵连无关模块；改动前后保持接口/行为兼容或明确兼容策略。  
4. **可读性/可维护性优先**：新增功能必须反复审视代码可读性与可维护性，优先在现有基础上扩展，而非重复造轮子或堆叠复杂逻辑。  

## 测试与验证  
1. **先做 Code Review（强制）**：完成改动后先进行自查 code review（对照需求与差异、检查潜在风险与回归点），确认无明显问题再开始测试与验证。  
2. **补充分测试例（UI）**：为新增功能补充对应的 UI 自动化用例（放入 `tests/ui/`），覆盖核心正反向场景。  
3. **后端接口测试**：为新增或改动的后端 API 编写/更新必要的接口测试用例（建议放入 `tests/api/`），覆盖鉴权、权限、正反向参数、幂等/并发等关键路径，并运行验证。  
4. **回归范围**：根据影响范围选择并运行相关 UI 自动化与 API 用例，确保旧功能不受影响（权限/端口/网络需求均可申请）。  
5. **旧用例同步**：若新功能改动会影响旧功能逻辑，需要同步调整/补充原有自动化用例，保持覆盖的正确性。  
6. **验证记录**：保存本地运行命令与结果（通过/失败原因）；如有失败需复盘并重跑直至通过。  
7. **执行模式**：默认使用 headless 运行 Playwright；如需 headed 模式（例如排查视觉/遮罩问题），须先征得需求方确认后再执行。  
8. **数据库隔离（强制）**：任何“会写入数据”的自动化测试/手工造数/调试验证，必须使用测试数据库，禁止对正式数据库造数或清理数据。  
   - 正式数据库：`data/app.db`（不设置 `APP_DB_FILE` 时默认使用，或显式 `APP_DB_FILE=app.db`）。  
   - 测试数据库：`data/apitest.db`（建议统一使用，或使用其它 `data/*test*.db` 文件；但必须与 `app.db` 隔离）。  
   - 建议启动命令：`APP_DB_FILE=apitest.db uvicorn backend.main:app --reload --host 0.0.0.0 --port 8080`（测试）；`APP_DB_FILE=app.db uvicorn ...`（正式/日常使用）。  

## 提交流程  
1. **需求登记**：新增功能完成后，必须在 `FEATURE_LOG.md` 记录功能描述、操作方式、使用效果、新增内容等关键信息；若后续调整同一需求细节，也需同步更新该文档。  
2. **通知发送**：完成开发与测试后调用 `python3 notify_feishu.py` 发送通知（脚本已配置，可直接使用；如需提权/网络访问可申请）。  
3. **结果回报**：向需求方/团队说明：  
   - 影响范围（模块/接口/页面）  
   - 测试执行情况（用例列表与结果）  
   - 是否复用了既有接口/组件（若未复用需说明原因）  
   - 最终完成进度/状态  

## 操作清单（执行时逐项勾选）  
- [ ] 评估并复用现有逻辑/接口；如需新增组件，设计为可复用形态  
- [ ] 完成改动后先进行自查 code review，再开始测试与验证  
- [ ] 编写或更新相关 UI 自动化用例  
- [ ] 编写或更新相关后端 API 接口测试用例，并执行验证  
- [ ] 根据影响范围运行 UI 自动化与 API 测试并记录结果  
- [ ] 调用 `python3 notify_feishu.py` 发送通知  
- [ ] 在 `FEATURE_LOG.md` 登记/更新需求信息  
- [ ] 输出影响范围、测试情况、复用情况与完成进度  

## 其他说明  
- 如需新增资源/示例，请按仓库规范放置（优先与 `index.html` 同级或 `assets/` 目录）。  
- 若开发中发现现有逻辑可抽象为通用组件，应优先重构为可复用单元，再接入新功能。  
